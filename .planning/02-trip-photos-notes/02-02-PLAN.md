---
phase: 02-trip-photos-notes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/bia/meta.json
autonomous: true
requirements:
  - DATA-01
  - MEDIA-01
  - MEDIA-02

must_haves:
  truths:
    - "data/bia/meta.json exists and is valid JSON matching the meta.json schema"
    - "media/bia/ directory exists"
    - "The wiring between meta.json photo filenames and media/bia/ is verifiable"
  artifacts:
    - path: "data/bia/meta.json"
      provides: "Sample meta.json for the bia (Belle-Ile & Brittany) trip"
      contains: "notes"
    - path: "media/bia/"
      provides: "Media directory for bia trip photos"
  key_links:
    - from: "data/bia/meta.json photos[].filename"
      to: "media/bia/{filename}"
      via: "PhotoCarousel img src path"
      pattern: "media/bia/"
---

<objective>
Create the sample data files needed to prove the Phase 2 pipeline end-to-end: a valid meta.json for the 'bia' trip and the media/bia/ directory containing a real test photo.

Purpose: Without at least one meta.json and one real image, MEDIA-01 and MEDIA-02 cannot be verified — the PhotoCarousel real-image branch would never execute. This plan makes the wire testable.
Output: data/bia/meta.json, media/bia/ directory, and one real photo file in media/bia/.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md

<interfaces>
<!-- meta.json schema from CLAUDE.md and RESEARCH.md -->

meta.json schema (all fields optional except at least one of notes/photos makes it useful):
```json
{
  "title": "Optional display title override — if absent, trip.name from data.js is used",
  "notes": "Trip notes text shown in stats panel blurb. Plain text, no markdown. 1-3 sentences.",
  "photos": [
    { "filename": "exact-filename.jpg", "caption": "Caption shown below the photo" }
  ]
}
```

Photo files go in media/{trip-id}/{filename} — referenced by filename only in meta.json.
Fetch path built by PhotoCarousel: `media/${trip.id}/${photo.filename}`

Trip bia data from data.js:
- id: 'bia'
- name: 'Belle-Ile & Brittany'
- blurb: 'A week on the wild Atlantic coast. Clifftop walking on the GR34 around the Crozon peninsula, then five days circling Belle-Ile-en-Mer on the GR 340.'
- country: 'France'
- months: 'May 2023'

Existing data/ folder structure (geographic names — DO NOT TOUCH these):
data/brittany/   ← GPX files for bia trip. Leave alone.
data/corsica/    ← GPX files for rab. Leave alone.
(etc.)

New folders to create:
data/bia/        ← for meta.json (NEW — trip-id-based)
media/bia/       ← for photo files (NEW)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/bia/ directory and meta.json</name>
  <files>data/bia/meta.json</files>

  <read_first>
    - CLAUDE.md (per-trip data section — meta.json schema)
    - .planning/02-trip-photos-notes/02-RESEARCH.md (Pattern 5: meta.json file format; sample meta.json in Code Examples section)
  </read_first>

  <action>
Create the directory `data/bia/` and write `data/bia/meta.json` with this exact content:

```json
{
  "title": "Belle-Ile & Brittany",
  "notes": "A week on the wild Atlantic coast. Clifftop walking on the GR34 around the Crozon peninsula, then five days circling Belle-Ile-en-Mer on the GR 340. The western cliffs of Belle-Ile in evening light are the kind of view that stays with you.",
  "photos": [
    { "filename": "001-clifftop.jpg", "caption": "clifftop above the Atlantic" },
    { "filename": "002-village.jpg", "caption": "last village before the descent" }
  ]
}
```

Notes on this content:
- `title` matches the trip name from data.js — will not visually change anything, but exercises the title override path
- `notes` is a slightly expanded version of the data.js blurb — verifiably different so the override is detectable
- `photos` lists two filenames that will be created in Task 2
- All JSON is valid (no trailing commas, valid UTF-8)

Do NOT create or modify any files in data/brittany/ or any other existing geographic folder.
  </action>

  <verify>
    - File exists: `ls data/bia/meta.json`
    - Valid JSON: parse the file content and confirm no JSON errors
    - Contains required keys: `grep -c "notes" data/bia/meta.json` returns 1
    - Contains photos array: `grep -c "filename" data/bia/meta.json` returns 2
  </verify>

  <acceptance_criteria>
    - `ls data/bia/meta.json` exits 0 (file exists)
    - JSON is parseable (no syntax errors) — validate by reading and confirming structure matches schema
    - `grep -c "\"notes\"" data/bia/meta.json` returns 1
    - `grep -c "\"filename\"" data/bia/meta.json` returns 2
    - `grep -c "001-clifftop.jpg" data/bia/meta.json` returns 1
    - `grep -c "002-village.jpg" data/bia/meta.json` returns 1
    - data/brittany/ directory is untouched (verify with `ls data/brittany/`)
  </acceptance_criteria>

  <done>data/bia/meta.json exists, is valid JSON, references two photo filenames, and has distinct notes text that differs from the data.js blurb.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 created data/bia/meta.json. This task requires placing real photo files in media/bia/ so the carousel's real-image branch is fully testable end-to-end.

Claude cannot create real JPEG photos — image binary files must come from the developer. The files needed are:
- media/bia/001-clifftop.jpg
- media/bia/002-village.jpg

These filenames must exactly match what's listed in data/bia/meta.json.

If you don't have photos ready right now, you can use any two JPEG files renamed to these filenames — placeholder stand-ins are fine for testing the wiring. The goal is that a real `<img>` element renders without hitting the onError handler.
  </what-built>
  <how-to-verify>
1. Create directory `media/bia/` if it doesn't exist
2. Copy or download two JPEG images and name them:
   - `media/bia/001-clifftop.jpg`
   - `media/bia/002-village.jpg`
3. Serve the repo root: `python -m http.server 8080`
4. Open http://localhost:8080 in a browser
5. Click the "Belle-Ile & Brittany" trip marker on the map
6. Confirm in the stats panel: the blurb reads "...The western cliffs of Belle-Ile in evening light are the kind of view that stays with you." (the extended notes text from meta.json, not the shorter data.js blurb)
7. Confirm in the carousel: real photos appear (not coloured SVG placeholder art), caption text reads "clifftop above the Atlantic" and "last village before the descent"
8. Carousel subtitle reads "2 photos" (not the "drop real images into..." placeholder text)
9. Click any other trip — stats and carousel revert to data.js blurb and placeholder SVGs
  </how-to-verify>
  <resume-signal>Type "photos added" when media/bia/ contains both JPEG files and you've verified the carousel shows real images. If you want to defer photos and only verify the notes/blurb wiring now, type "notes only" and note that MEDIA-01 and MEDIA-02 remain unverified until photos are added.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static files → browser | meta.json and image files served from same origin — no cross-origin risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | meta.json notes content | accept | Personal site; notes are authored content, not sensitive data |
| T-02-05 | Tampering | Photo files in media/ | accept | Static files, author-controlled; no server-side processing |
</threat_model>

<verification>
After Task 1 and the human checkpoint:
- `ls data/bia/meta.json` — file exists
- `ls media/bia/` — directory exists with at least one .jpg file
- `python -m http.server 8080` → visit http://localhost:8080 → click Belle-Ile & Brittany:
  - Stats blurb contains "western cliffs" (from meta.json notes, not data.js blurb)
  - Carousel subtitle shows "2 photos" (not placeholder copy)
  - Carousel shows real images (or tan background fallback if filenames don't match)
</verification>

<success_criteria>
- data/bia/meta.json is valid JSON with notes and photos fields
- media/bia/ directory exists
- At least one real JPEG in media/bia/ matching a filename in meta.json
- Full pipeline verified in browser: meta.notes appears in stats panel for bia trip
</success_criteria>

<output>
After completion, create `.planning/phases/02-trip-photos-notes/02-02-SUMMARY.md`
</output>
