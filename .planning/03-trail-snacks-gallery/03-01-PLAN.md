---
plan: "03-01"
phase: "3"
title: "Snacks gallery page — snacks.html + snacks.jsx"
status: pending
wave: 1
---

# Plan 03-01 — Snacks gallery page

## Goal

Create `snacks.html` and `snacks.jsx`: a standalone CDN-only React page that fetches `snacks.json`, sorts snacks by average (joint) score descending, and renders a responsive grid of snack cards. Include a nav link back to the map page. Also create the `media/snacks/` directory placeholder.

## Context

- Same CDN stack as index.html: React 18.3.1 + Babel 7.29.0 UMD, no Leaflet, no build step
- Same design system: CSS custom properties (`--green`, `--tan`, `--offwhite`, etc.), Syne/Nunito/JetBrains Mono fonts
- `snacks.json` schema (from CLAUDE.md):
  ```json
  { "id": "slug", "name": "...", "country": "...", "photo": "slug.jpg",
    "ben": { "score": 8, "note": "One sentence." },
    "janna": { "score": 7, "note": "One sentence." } }
  ```
- Photos at `media/snacks/<photo filename>` — may not exist yet (show a placeholder SVG on error)
- Joint score = `(ben.score + janna.score) / 2`, rounded to one decimal
- Sort order: highest joint score first

## Tasks

### Task 1 — Create `media/snacks/.gitkeep`

Create an empty `.gitkeep` file so the directory is tracked by git.

```
media/snacks/.gitkeep
```

### Task 2 — Create `snacks.html`

Full file. Copy the `:root` CSS variables block from index.html exactly. Then add page-specific styles.

**HTML shell:**
```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Trail Snacks — Wanderlog</title>

<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=Nunito:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />

<style>
/* copy :root block from index.html verbatim */
/* ... */

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  font-family: var(--serif);
  background: var(--offwhite);
  color: var(--green-deep);
  -webkit-font-smoothing: antialiased;
  min-height: 100vh;
}

/* HEADER */
.sn-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 36px 18px;
  background: var(--offwhite);
  border-bottom: 1px solid var(--rule);
  position: sticky;
  top: 0;
  z-index: 100;
}
.sn-brand-title {
  font-family: var(--display);
  font-weight: 800;
  font-size: 1.6rem;
  letter-spacing: 2px;
  color: var(--green);
  text-transform: uppercase;
  line-height: 1;
}
.sn-brand-sub {
  font-family: var(--serif);
  font-style: italic;
  font-size: 0.88rem;
  color: var(--tan-deep);
  margin-top: 4px;
}
.sn-nav-link {
  font-family: var(--serif);
  font-size: 0.95rem;
  font-weight: 700;
  color: var(--green);
  background: var(--offwhite);
  border: 1.5px solid var(--rule-strong);
  border-radius: 999px;
  padding: 8px 20px;
  cursor: pointer;
  text-decoration: none;
  transition: all 0.2s;
  display: flex; align-items: center; gap: 8px;
  letter-spacing: 0.3px;
}
.sn-nav-link:hover { background: var(--green); color: var(--offwhite); }

/* GALLERY */
.sn-page { padding: 40px 36px 80px; }
.sn-page-title {
  font-family: var(--display);
  font-size: 1.1rem;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--tan-deep);
  margin-bottom: 28px;
}
.sn-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 28px;
}

/* CARD */
.sn-card {
  background: #fff;
  border: 1px solid var(--rule);
  border-radius: 12px;
  overflow: hidden;
  box-shadow: var(--shadow-soft);
  transition: box-shadow 0.2s, transform 0.2s;
}
.sn-card:hover {
  box-shadow: var(--shadow-card);
  transform: translateY(-3px);
}
.sn-card-photo {
  width: 100%;
  aspect-ratio: 4 / 3;
  object-fit: cover;
  display: block;
  background: var(--tan);
}
.sn-card-body { padding: 16px 18px 18px; }
.sn-card-name {
  font-family: var(--display);
  font-weight: 700;
  font-size: 1.1rem;
  color: var(--green);
  line-height: 1.2;
}
.sn-card-country {
  font-family: var(--mono);
  font-size: 0.62rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--tan-deep);
  margin-top: 4px;
}
.sn-card-scores { margin-top: 14px; display: flex; flex-direction: column; gap: 8px; }
.sn-score-row { display: flex; align-items: flex-start; gap: 10px; }
.sn-score-badge {
  font-family: var(--mono);
  font-size: 0.78rem;
  font-weight: 700;
  min-width: 42px;
  text-align: center;
  padding: 3px 6px;
  border-radius: 6px;
  background: var(--offwhite-2);
  color: var(--green);
  flex-shrink: 0;
  border: 1px solid var(--rule);
}
.sn-score-who {
  font-family: var(--mono);
  font-size: 0.6rem;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--tan-deep);
  flex-shrink: 0;
  padding-top: 4px;
  min-width: 34px;
}
.sn-score-note {
  font-family: var(--serif);
  font-style: italic;
  font-size: 0.82rem;
  color: var(--green-deep);
  line-height: 1.35;
}
.sn-card-avg {
  margin-top: 14px;
  padding-top: 12px;
  border-top: 1px solid var(--rule);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.sn-avg-label {
  font-family: var(--mono);
  font-size: 0.62rem;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--tan-deep);
}
.sn-avg-score {
  font-family: var(--display);
  font-size: 1.4rem;
  font-weight: 800;
  color: var(--green);
}

/* LOADING / ERROR */
.sn-loading {
  text-align: center;
  padding: 80px 0;
  font-family: var(--serif);
  font-style: italic;
  color: var(--tan-deep);
}
</style>
</head>
<body>
<div id="root"></div>

<script src="https://unpkg.com/react@18.3.1/umd/react.development.js" integrity="sha384-hD6/rw4ppMLGNu3tX5cjIb+uRZ7UkRJ6BPkLpg4hAu/6onKUg4lLsHAs9EBPT82L" crossorigin="anonymous"></script>
<script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js" integrity="sha384-u6aeetuaXnQ38mYT8rp6sbXaQe3NL9t+IBXmnYxwkUI2Hw4bsp2Wvmx4yRQF1uAm" crossorigin="anonymous"></script>
<script src="https://unpkg.com/@babel/standalone@7.29.0/babel.min.js" integrity="sha384-m08KidiNqLdpJqLq95G/LEi8Qvjl/xUYll3QILypMoQ65QorJ9Lvtp2RXYGBFj1y" crossorigin="anonymous"></script>

<script type="text/babel" src="snacks.jsx"></script>
</body>
</html>
```

### Task 3 — Create `snacks.jsx`

```jsx
const { useState, useEffect } = React;

function avgScore(snack) {
  return (snack.ben.score + snack.janna.score) / 2;
}

function PhotoImg({ snack }) {
  const [errored, setErrored] = useState(false);
  if (errored) {
    return (
      <svg className="sn-card-photo" viewBox="0 0 400 300" preserveAspectRatio="none">
        <rect width="400" height="300" fill="var(--tan)" />
        <text x="200" y="158" textAnchor="middle" fontFamily="var(--mono)"
              fontSize="13" fill="rgba(247,247,239,0.7)" letterSpacing="2">NO PHOTO</text>
      </svg>
    );
  }
  return (
    <img
      className="sn-card-photo"
      src={`media/snacks/${snack.photo}`}
      alt={snack.name}
      onError={() => setErrored(true)}
    />
  );
}

function SnackCard({ snack }) {
  const avg = avgScore(snack);
  return (
    <div className="sn-card">
      <PhotoImg snack={snack} />
      <div className="sn-card-body">
        <div className="sn-card-name">{snack.name}</div>
        <div className="sn-card-country">{snack.country}</div>
        <div className="sn-card-scores">
          <div className="sn-score-row">
            <div className="sn-score-badge">{snack.ben.score}/10</div>
            <div className="sn-score-who">Ben</div>
            <div className="sn-score-note">{snack.ben.note}</div>
          </div>
          <div className="sn-score-row">
            <div className="sn-score-badge">{snack.janna.score}/10</div>
            <div className="sn-score-who">Janna</div>
            <div className="sn-score-note">{snack.janna.note}</div>
          </div>
        </div>
        <div className="sn-card-avg">
          <div className="sn-avg-label">Joint score</div>
          <div className="sn-avg-score">{avg.toFixed(1)}</div>
        </div>
      </div>
    </div>
  );
}

function SnackGallery() {
  const [snacks, setSnacks] = useState(null);
  const [error, setError] = useState(false);

  useEffect(() => {
    fetch('snacks.json')
      .then(r => { if (!r.ok) throw new Error(); return r.json(); })
      .then(data => {
        const sorted = [...data].sort((a, b) => avgScore(b) - avgScore(a));
        setSnacks(sorted);
      })
      .catch(() => setError(true));
  }, []);

  if (error) return <div className="sn-loading">Could not load snacks.json</div>;
  if (!snacks) return <div className="sn-loading">Loading…</div>;

  return (
    <main className="sn-page">
      <div className="sn-page-title">Rated {snacks.length} trail snacks</div>
      <div className="sn-grid">
        {snacks.map(s => <SnackCard key={s.id} snack={s} />)}
      </div>
    </main>
  );
}

function App() {
  return (
    <div>
      <header className="sn-header">
        <div>
          <div className="sn-brand-title">WANDERLOG</div>
          <div className="sn-brand-sub">Trail snack ratings — Ben &amp; Janna</div>
        </div>
        <a href="index.html" className="sn-nav-link">
          <span aria-hidden>←</span> Map
        </a>
      </header>
      <SnackGallery />
    </div>
  );
}

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
```

## Success Criteria

- `snacks.html` opens in browser and renders the React app
- With a populated `snacks.json`, cards appear sorted highest joint score first
- With no photo file present, a tan SVG placeholder renders (no broken img icon)
- "← Map" link navigates to index.html
- Card layout: photo, name, country, two score rows, avg score footer
- Grid is responsive (auto-fill minmax 280px)

## Commit

```
feat(03-01): snacks gallery page — snacks.html, snacks.jsx, media/snacks/
```
